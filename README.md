# بوت الموسوعة الحديثية 🤖

بوت Discord للبحث في الموسوعة الحديثية من موقع الدرر السنية.

## 📋 المميزات

- ✅ البحث التلقائي عن أي موضوع في الموسوعة الحديثية
- ✅ عرض النتائج بشكل منظم وجميل
- ✅ يعمل تلقائياً عند كتابة أي موضوع
- ✅ دعم الأوامر المخصصة
- ✅ معالجة الأخطاء بشكل احترافي

## 🚀 التثبيت

### 1. تثبيت المكتبات المطلوبة:

```bash
pip install -r requirements.txt
```

### 2. إعداد الإعدادات:

**⚠️ مهم: ملف `config.py` محمي ولا يُرفع على GitHub لأسباب أمنية**

1. انسخ ملف `config.example.py` إلى `config.py`:
```bash
copy config.example.py config.py
```

2. افتح ملف `config.py` واملأ القيم الحقيقية:
- `TOKEN`: توكن البوت من Discord Developer Portal
- `CLIENT_ID`: معرف البوت

**ملاحظة:** ملف `config.py` موجود في `.gitignore` ولن يُرفع على GitHub

### 3. تشغيل البوت:

```bash
python main.py
```

## 📖 الاستخدام

### البحث التلقائي:
اكتب أي موضوع مباشرة في القناة وسيقوم البوت بالبحث تلقائياً:

```
الصلاة
```

### استخدام الأوامر:

**البحث:**
```
!بحث الصلاة
```

**المساعدة:**
```
!مساعدة
```

## 📁 هيكل المشروع

```
bot-zaid/
├── main.py              # الملف الرئيسي للبوت
├── config.py            # إعدادات البوت (لا يُرفع على GitHub)
├── config.example.py    # قالب الإعدادات (يُرفع على GitHub)
├── api_handler.py       # معالج API
├── utils.py             # أدوات مساعدة
├── commands.py          # أوامر البوت (اختياري)
├── requirements.txt     # المكتبات المطلوبة
├── README.md            # هذا الملف
└── .gitignore           # ملفات Git المستثناة
```

## ⚙️ الإعدادات

يمكنك تعديل الإعدادات في ملف `config.py`:

- `TOKEN`: توكن البوت
- `CLIENT_ID`: معرف البوت
- `COMMAND_PREFIX`: بادئة الأوامر (افتراضي: `!`)
- `MAX_RESULTS`: عدد النتائج القصوى للعرض (افتراضي: 5)

## 🔧 المتطلبات

- Python 3.8 أو أحدث
- discord.py 2.3.0 أو أحدث
- aiohttp 3.9.0 أو أحدث

## 📝 ملاحظات

- تأكد من تفعيل **Message Content Intent** في Discord Developer Portal
- **⚠️ احتفظ بـ Token سرياً ولا تشاركه مع أحد**
- ملف `config.py` محمي في `.gitignore` ولن يُرفع على GitHub
- استخدم `config.example.py` كقالب عند مشاركة المشروع
- البوت يستخدم API من موقع الدرر السنية

## 🔒 الأمان والخصوصية

### ⚠️ قبل رفع المشروع على GitHub:

**✅ Token محمي ولن يُنشر!**

- ✓ `config.py` موجود في `.gitignore` - **لن يُرفع**
- ✓ `config.example.py` فقط سيُرفع - **آمن (بدون Token حقيقي)**
- ✓ Token الخاص بك **لن يُنشر على GitHub**

### 📋 قائمة الملفات المحمية:
- `config.py` - **محمي** (يحتوي على Token الحقيقي)
- `.env` - **محمي**
- `*.log` - **محمي**

### ✅ الملفات الآمنة للرفع:
- جميع ملفات `.py` ما عدا `config.py`
- `config.example.py` - **آمن للرفع**
- `requirements.txt`
- `README.md`
- `.gitignore`

**راجع ملف `GITHUB_WARNING.md` لمزيد من التفاصيل الأمنية.**

## 🐛 حل المشاكل

### البوت لا يستجيب:
- تأكد من أن Token صحيح
- تأكد من تفعيل Message Content Intent
- تأكد من أن البوت لديه صلاحيات القراءة والكتابة في القناة

### خطأ في الاتصال بالAPI:
- تأكد من الاتصال بالإنترنت
- تحقق من أن رابط API يعمل

## 📄 الترخيص

هذا المشروع مفتوح المصدر.

## 👤 المطور

تم تطوير هذا البوت للبحث في الموسوعة الحديثية.
